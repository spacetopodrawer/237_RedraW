name: gnss_post_process
description: Application de post-traitement GNSS multiplateforme
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^5.3.1
  path_provider: ^2.0.15
  provider: ^6.0.5
  http: ^1.1.0
  flutter_map: ^5.0.0
  latlong2: ^0.9.0
  pdf: ^3.10.4
  share_plus: ^7.1.0
  url_launcher: ^6.1.14
  csv: ^5.0.2
  xml: ^6.3.0
  archive: ^3.4.9
  proj4dart: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true